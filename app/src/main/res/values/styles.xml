<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/myBouttonStyle</item>
    </style>

    <style name="myBouttonStyle" parent="android:Widget.Button">
        <item name="android:textColor">@color/colorStyle1</item>
        <item name="android:background">@drawable/formes</item>
        <item name="android:padding">10dp</item>
    </style>

</resources>
